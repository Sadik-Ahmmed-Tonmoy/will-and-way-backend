// enum NotificationType {
//   GENERAL
//   BOOKING_UPDATE
//   MESSAGE
//   GROUP_MESSAGE
//   SYSTEM_ALERT
// }


// model Notifications {
//   id           String           @map("_id") @id @default(auto()) @db.ObjectId
//   senderId     String                       @db.ObjectId
//   receiverId   String                       @db.ObjectId
//   title        String
//   body         String
//   additionData Json?
//   type         NotificationType
//   read         Boolean                      @default(false)
//   createdAt    DateTime                     @default(now())
//   updatedAt    DateTime                     @updatedAt
//   user         User             @relation(fields: [receiverId], references: [id], name: "Notifications", onDelete: Cascade)
//   sender       User             @relation(fields: [senderId], references: [id], name: "SenderNotifications", onDelete: Cascade)

//   @@map("notifications")
// }